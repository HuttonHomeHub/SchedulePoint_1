---
'@repo/api': minor
---

Each ghost in a revision comparison now carries the old side's activity `type`, so a client can place a milestone ghost where its diamond is drawn. The field is additive; `isMilestone` is unchanged.
