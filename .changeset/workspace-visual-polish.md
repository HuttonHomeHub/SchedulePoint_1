---
'@repo/web': minor
---

The workspace visual polish pass (product-owner direction, seven items). The shell goes full-bleed
and the canvas fully flush — the grey frame, the band's card chrome and the stage card are removed,
a knowing reversal of the 2026-08-24 redesign's framing by the same authority. The foot band gains
the chrome band's 3px amber rule mirrored along its top, copies the deck's content inset, and the
singular selection bar carries a SELECTION caption in the deck-caption style. The plan's facts
become two explicit pairs — Data date + Finish above, Activities + the critical count below. The
three right docks join the `panel` surface scope (they were `bg-card` on the page scope, the
ADR-0097 split-pair class), and a dock now pushes the canvas only: the foot spans the full width
beneath both, so opening Notes, Float paths or Health check no longer narrows the activities row.
The deck's group fold is removed — captions become static labels; the grouping was the value, the
disclosure was the cost.
