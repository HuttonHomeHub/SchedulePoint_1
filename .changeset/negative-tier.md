---
'@repo/web': patch
---

Add the seed catalogue's negative tier (`seed --tier negative`): the 18 hostile cases of the conformance fixture, attempted one write each against the real API, reporting observed behaviour against declared expectation. Run against a live instance, all 18 behave as the fixture requires — including the three the pure engine marks `todo` because they are API-boundary concerns it cannot own.
