---
'@repo/web': minor
---

Correctness pass, phases 2–4: the authenticated shell below `lg` is exercised by its first-ever
narrow journey — whose first run found and fixed the Explorer sheet painting with no ground at
all; a whole-plan export keeps its weekends at any scale (merged runs below the screen's zoom
floor); the exported diagram is the planner's picture — colour-by, over-allocation highlight,
baseline ghosts and isolate/filter dimming all export as shown; the canvas painter is set in the
product's own typeface (IBM Plex Sans), with the family derived from the stylesheet by a gate so
the next face change cannot silently miss the canvas; and the screenshot harness photographs
every screen again, recording a failed shot instead of abandoning the run.
