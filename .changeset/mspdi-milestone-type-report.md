---
'@repo/api': patch
'@repo/interchange': patch
---

An MS Project import now lists every milestone whose start-or-finish type it had to guess, since MS Project records only that a task is a milestone. The guess itself is unchanged.
