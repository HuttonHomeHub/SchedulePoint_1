# Technical debt register

Known shortcuts, gaps, and deferred decisions, with the intent to address them.
Keep this honest and current — undocumented debt is the expensive kind.

> Format: each item has a short description, why it exists, the risk if left,
> and a remediation intent. Remove items when resolved (note it in
> [DECISIONS.md](DECISIONS.md) or `CHANGELOG.md` if significant).

| #   | Item                                  | Why it exists                                                                                                                                                                                                                                                                                             | Risk                                                                                 | Remediation intent                                                                                                                                                  |
| --- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | **Web e2e is Chromium-first**         | The web entry point has landed (M1, A2): CI now runs `pnpm build` (api + web) and a Playwright journey (sign-up → shell → sign-out, with an axe check) against a real API + Postgres. The Playwright config defines chromium/firefox/webkit projects but coverage so far is exercised mainly on Chromium. | Firefox/WebKit-specific regressions may slip until those projects are routinely run. | Keep the cross-browser projects green as the UI grows; expand journeys per feature.                                                                                 |
| 2   | **Swagger CLI plugin disabled**       | The `@nestjs/swagger` CLI plugin generated a `metadata.ts` that tripped `noUnusedLocals`. OpenAPI is currently produced via explicit `@Api*` decorators (which works), so the plugin is optional.                                                                                                         | Without the plugin, DTO schemas must be annotated by hand.                           | Optionally re-enable `plugins: ["@nestjs/swagger"]` in `nest-cli.json` to auto-enrich schemas; verify the build stays green.                                        |
| 3   | **Observability wiring is partial**   | Structured logging + correlation IDs are implemented; OpenTelemetry metrics/traces (ADR-0013) and a backend are not yet wired.                                                                                                                                                                            | Limited metrics/traces until wired.                                                  | Add the OTel SDK + exporter and a collector per environment.                                                                                                        |
| 4   | **Async/cache/storage not wired**     | BullMQ (ADR-0009), Redis cache (ADR-0010), and object storage (ADR-0011) are designed but not yet added to the stack (no jobs/hot paths/files exist yet).                                                                                                                                                 | Patterns exist on paper only.                                                        | Add Redis/MinIO to compose and the modules when the first job/cached read/file lands.                                                                               |
| 5   | **Hosting platform undecided**        | Not enough information at foundation stage.                                                                                                                                                                                                                                                               | Deployment specifics (scaling, networking, secrets) unresolved.                      | Evaluate managed-container vs. Kubernetes options; record an ADR.                                                                                                   |
| 6   | **No business/domain code yet**       | Deliberate — the schema has no models and the reference feature is a **non-shipping template** (`apps/api/examples/`, ADR-0014), kept honest by the CI template-verify job (`scripts/verify-template.sh`).                                                                                                | Docs/template describe intent; real domain still to come.                            | Build features from the reference template when you start an application; delete the template once real features make it redundant.                                 |
| 7   | **Performance targets are estimates** | Set before real workloads exist.                                                                                                                                                                                                                                                                          | Targets may be wrong.                                                                | Re-baseline against real metrics once deployed.                                                                                                                     |
| 8   | **CSP not finalised for web**         | App origins/assets unknown until features land.                                                                                                                                                                                                                                                           | Weaker XSS mitigation than achievable.                                               | Tighten the nginx/`helmet` CSP once origins are known.                                                                                                              |
| 9   | **Auth library relatively young**     | Better Auth is now wired into the `AuthContextService` seam (email + password, cookie sessions; ADR-0003, A1). Ecosystem maturity remains a watch item.                                                                                                                                                   | Ecosystem maturity risk.                                                             | Monitor releases/advisories; keep the boundary swappable behind the seam.                                                                                           |
| 10  | **ESLint pinned to v9**               | ESLint 10 is available, but `eslint-plugin-import`, `eslint-plugin-jsx-a11y`, and `eslint-plugin-react` still cap their peer range at ESLint 9. Dependabot's major bump is ignored (see `.github/dependabot.yml`).                                                                                        | Missing ESLint 10 features/fixes until the plugins catch up.                         | Remove the `eslint` major-ignore and bump the ESLint group once the plugins publish v10-compatible releases.                                                        |
| 11  | **Prisma pinned to v6**               | Prisma 7 removes `url` from the datasource block and requires a driver adapter + `prisma.config.ts` — a deliberate migration, not a routine bump. The major is ignored in Dependabot.                                                                                                                     | Missing Prisma 7 improvements until migrated.                                        | Do the Prisma 7 migration deliberately (driver adapter, `prisma.config.ts`, `PrismaService` wiring) — worth an ADR — then un-ignore.                                |
| 12  | **CodeQL off on private repos**       | Code scanning uploads are free on public repos but need GitHub Advanced Security (paid) on private ones. The CodeQL workflow is gated to public repos (`.github/workflows/codeql.yml`) so apps generated from this template stay green instead of failing on every push.                                  | No static-analysis (CodeQL) scanning on private repos.                               | Enable GitHub Advanced Security, or make the repo public, to activate CodeQL; Dependabot, secret scanning, and the security-reviewer agent remain active meanwhile. |

| 13 | **TypeScript pinned to v5** | TypeScript 7 (the native compiler) removed `baseUrl` and `moduleResolution: node10` from tsconfig, which the shared presets rely on for the `@/` and `@repo/*` path aliases. The major is ignored in Dependabot. | Missing TypeScript 7 speed/features until migrated. | Migrate the tsconfig presets (drop `baseUrl`, move to `paths`/`bundler` resolution), verify nest/vite resolution, then un-ignore. |

| 14 | **Append-only audit log missing** | No append-only audit-log framework exists yet (flagged in the A1 and C1 security reviews), so security-sensitive events are only in operational logs + row audit columns: (a) authentication events (sign-up/in/out); (a2) membership role changes / removals and invitation create/revoke/accept (who did what to which org, before→after); (b) Better Auth's rate-limit store is in-process memory — per-replica once scaled; (c) the `accounts` OAuth token columns are unencrypted at rest (harmless today — only email+password is enabled). | No tamper-evident audit trail for permission changes / deletions / auth; rate limits weaken under horizontal scaling; token columns unencrypted before OAuth ships. | Build an `AuditLog` model + service (SECURITY_STANDARDS.md) and write auth + membership/invitation events to it; back the rate-limit store with Redis (ADR-0010) before scaling out; add field-level encryption for OAuth token columns before enabling social providers. |

| 15 | **OpenAPI accuracy gaps** | Repo-wide, from the B2 API review: (a) `201 Create` responses don't set a `Location` header (`docs/API.md` asks for one) — present in the reference template too; (b) the `@Api*Response` decorators declare the bare DTO, not the `{ data }`/`{ data, meta }` envelope the `TransformInterceptor` actually returns. | Generated OpenAPI is slightly inaccurate about response shape and `Location`. | Add a shared `@ApiDataResponse()`/`@ApiPaginatedResponse()` swagger helper and a `Location` header on creates; backport to the reference template so the two stay in step (ADR-0015). |

| 16 | **Email verification not enforced (invitation identity)** | From the C2 security review: `AUTH_REQUIRE_EMAIL_VERIFICATION` defaults `false` because no verification-email loop exists yet (only a logging `MailService` stub). Invitation acceptance grants org membership on an email-match check that only proves mailbox ownership when verification is on (ADR-0016 §5). | An adversary who registers an account for a matching address **and** holds the one-time invite token could accept; account-squatting can also block the real invitee's sign-up. Alpha-only, deliberately accepted. | Build the verification-email flow through the `MailService` port, then set `AUTH_REQUIRE_EMAIL_VERIFICATION=true` — the accept-time `emailVerified` gate and Better Auth enforcement are already wired to that flag, so no further code change is needed. Consider a stricter per-route throttle on `POST /invitations/preview` \| `/accept` at the same time. |

| 17 | **Members UI a11y polish (non-blocking)** | From the C3 accessibility review, after the blocking contrast/focus/live-region fixes: (a) controls use the native `disabled` attribute while a mutation is pending, so keyboard focus drops to `<body>`; (b) the `Dialog` `description` isn't linked via `aria-describedby`; (c) modal initial focus lands on the ✕ close button rather than the first field; (d) no `aria-live` success confirmation for role change / removal / link-copy; (e) light `muted-foreground` (4.73:1) and the sm remove button (36px vs. preferred 44px touch target) are within-spec but tight. | Minor friction for keyboard/AT users; all currently meet AA. | Prefer `aria-disabled` + pointer-events guard over native `disabled` on pending controls; add `aria-describedby` to `Dialog`; set an explicit initial-focus target; add a shared polite toast for success; revisit the tight tokens/targets when the notifications component lands. |

| 18 | **CI image job has no layer cache** | The `image` job (`.github/workflows/ci.yml`, ADR-0020) builds both container images from scratch on every run: the Dockerfiles' `--mount=type=cache,id=pnpm` BuildKit cache is local to an ephemeral runner and isn't persisted across CI runs, and the job invokes `docker compose … --build` directly without a GHA-backed buildx cache. | Slower CI (full `pnpm install` + `prisma generate` + `tsc` + `vite build` each run); more Action minutes. | Wire `docker/setup-buildx-action` + `cache-from`/`cache-to: type=gha` (or `docker buildx bake`) so image layers persist across runs. |

| 19 | **`order` pagination param is accepted but ignored** | From the B1 API/security reviews: `PaginationQueryDto` advertises `order: asc\|desc` (and `docs/API.md` promises it), but the list repositories (`org-member.repository.ts`, `client.repository.ts`) hardcode `orderBy: [{ createdAt: 'asc' }, { id: 'asc' }]`. Repo-wide, not new to clients. | The generated OpenAPI documents a query param that has no effect; a client passing `order=desc` is silently ignored. | Either honour `order` in the shared list repositories (flip the keyset direction) or drop `order` from `PaginationQueryDto` for endpoints that don't support it; align `docs/API.md`. |

| 20 | **Keyset cursor is resolved before the scope filter** | From the C1 security review (pre-existing shared behaviour, also in `client.repository.ts`/`org-member.repository.ts`): the list repositories pass `cursor: { id }` to Prisma, which resolves that row by global `id` uniqueness before the org/client `WHERE` filter is applied. A cursor value copied from another org's row is therefore accepted as a valid pagination anchor. | None exploitable — the returned page is still filtered by `organizationId`/`clientId`, so no cross-scope rows leak; only the anchor position is honoured. Cosmetic/robustness. | Validate the cursor belongs to the resolved scope (or use an opaque signed cursor) in the shared list-repository helper when one is extracted; capture the standard in an ADR/`docs/API.md` pagination note. |

| 21 | **Systemic web-a11y & polish follow-ups (E1 reviews)** | Non-blocking items from the E1 component/UX/accessibility reviews that are pre-existing or systemic, so best fixed once at the primitive/shell level rather than per-feature: (a) no required-field indicator in the shared `Form`/`TextField` primitive (affects every form — sign-in/up/invite/create-org too); (b) no focus-to-heading / `document.title` update on client-side route navigation (router/`AuthedLayout` level); (c) `sm` ghost row-action buttons are 36px (below the 44px touch-target preference), shared with `MembersTable`; (d) no shared `EmptyState` primitive (icon + copy + action) — empty states are text-only; (e) from the E2 review: no shared `DateField`/`SelectField` form primitives — plans hand-compose a `<Select>`+`Label` (also in `InviteMemberDialog`) and a `TextField type="date"`, both of which the CPM/GPM feature set will need repeatedly (baseline/actual/constraint dates, status-like enums). | Minor friction for keyboard/AT and touch users; all current states still meet WCAG 2.2 AA. | Add a required-indicator to the `Form` primitive; add a route-change focus/title manager once in `AuthedLayout`; introduce `EmptyState`, `SelectField`, and `DateField` primitives (folding the calendar-date wire↔display contract into `DateField`) and bump the row-action target size when the design system is next revised. |

| 22 | **Recycle-bin list over-fetches across three tables** | From the E3 API review (`docs/DECISIONS.md`, 2026-07-10): `GET /organizations/:orgSlug/deleted` keyset-paginates the union of the `client`/`project`/`plan` tables by fetching each table's top `limit + 1` and merge-sorting in the service (`recycle-bin.service.ts`), so a page reads up to `3 × (limit + 1)` rows (≤303 at the `limit=100` cap). Like every other web list, the recycle-bin screen also fetches only the first page (`meta.hasMore`/`nextCursor` are returned by the API but the web `apiFetch` discards `meta`) — shared with clients/projects/plans (see #19/#20). | Slightly more rows read per page than returned; the web UI silently shows only the first page of deleted items. | If the recycle bin ever gets hot, replace the merge with a single raw `UNION ALL … ORDER BY (deleted_at, id) LIMIT` keyset query; when a shared paginated list hook lands on the web, wire "load more"/infinite scroll through the `{ data, meta }` envelope for all lists at once. |

| 23 | **App-header nav has no responsive collapse** — _addressed (ADR-0029; navigator on by default)_ | From the E3 UX review: the org nav (`apps/web/src/components/layout/app-header.tsx`) is a single flex row that grew to four items (Overview / Clients / Members / Recently deleted). `docs/FRONTEND_ARCHITECTURE.md` documents the intended shell as "nav collapses to a drawer/sheet below `lg`", which isn't built. E3 mitigated the immediate overflow by making the nav shrink and scroll horizontally (`min-w-0 flex-1 overflow-x-auto`, links `whitespace-nowrap`) so it never pushes the page into horizontal overflow, but a horizontally-scrolling nav strip is a stopgap, not the intended mobile pattern. **The persistent app-shell (ADR-0029) is landing this:** M1 added the shell — a Project Explorer rail pinned on `lg`+ and an off-canvas drawer (with a header menu button) below `lg` — behind `VITE_NAV_TREE` (default off). The primary navigation moves into the rail/drawer once the flag flips on at M2. | On narrow viewports the primary nav becomes a scroll strip rather than a proper menu; discoverability of later items is weaker. Every new nav item makes the row tighter. | Complete the navigator (M2), flip `VITE_NAV_TREE` on, and fold the header org-nav items into the rail; move low-frequency maintenance items (e.g. "Recently deleted") into an org-settings/admin area once one exists. |

| 24 | **TSLD free-2D drag follow-ups (M4 4.2 reviews)** | From the 4.2 reviews. **(a) RESOLVED in M5 5.2** — the rapid `Alt+↑/↓` key-repeat self-race is fixed by the coalesce-and-serialize nudge (one net write per burst, live version). **(b) RESOLVED in M5 5.2** — a lane-boundary `Alt+↑` at lane 0 now announces "Already in the top lane." **(c) RESOLVED** — a same-lane time overlap (which auto-arrange never makes but a manual lane drop can) is now flagged: a pure `laneOverlapIds` pass at the mapping seam sets `RenderActivity.laneOverlap` on both bars, the painter draws a stacked-squares badge (a shape cue, WCAG 1.4.1, named in the legend), and `describeActivity` speaks "overlaps another activity in its lane". Still open: (d) the route seam that picks `useRepositionLane` vs `useUpdateActivity` in `plan-detail.tsx` is only covered indirectly — no route-level test harness exists yet; (e) the `onSettled` hooks repeat the `listByPlan`+`detail` invalidation pair, ripe for an `invalidateActivity` helper. | Minor; all still WCAG 2.2 AA. | Add a route-level test harness + the seam test in a follow-up; extract `invalidateActivity` when a fourth minimal-PATCH hook appears. |

| 25 | **TSLD edit-keymap: pre-enablement checks + flag-on Playwright journey** | From M5 5.2. **(a) Pre-enablement gate (a11y sign-off condition):** that `Alt+←/→` (the SNET time nudge) does NOT trigger native Back/Forward history navigation is now **asserted automatically on Chromium** by the flag-on `keyboard-edit.spec.ts` (M6); the **Firefox/Safari/Edge** sweep was **MANUALLY CONFIRMED PASSING (2026-07-12)** — the diagram keeps focus and does not navigate Back/Forward on any of the three — clearing the last pre-enablement gate; `VITE_TSLD_EDITING` can now be enabled in a shared env (then `PLAN_EDIT_LOCK_ENFORCED`, per ADR-0028 §9). See `docs/runbooks/tsld-editing-enablement.md` and the flag comment (`apps/web/src/config/env.ts`). **(b) RESOLVED in M6** — the flag-on Playwright harness (`playwright.edit.config.ts` + `test:e2e:edit`, wired into CI) serves the app with `VITE_TSLD_EDITING=true`/`VITE_PLAN_EDIT_LOCK=true` and the API enforcing, and `keyboard-edit.spec.ts` drives the edit keymap end-to-end in a real browser. **(c) RESOLVED in M6** — the coalesced-nudge unmount flush now awaits the in-flight write then flushes the queued delta; regression-tested for both the resolve and reject settle branches. | RESOLVED — the manual cross-browser `Alt+←/→` history-suppression sweep passed (2026-07-12); no residual. | None — all pre-enablement checks are green; the editing flags are cleared to enable. |

| 26 | **Edit-lock heartbeat resolves the holder's own profile every beat** — **RESOLVED in M6.** | From the edit-lock M1 backend-performance review (ADR-0028). `buildStatus` used to `findActors` (a `users.findMany`) to resolve the holder's `{name, email}` every beat, even though on a successful heartbeat the holder is the caller. **Fixed:** `Principal` now carries the caller's `name`/`email` (already loaded by `getSession`), and `buildStatus` supplies the self-holder actor locally, resolving only a non-self pending requester — so the common no-requester beat issues **zero** `users` queries. | Resolved — no per-editor round-trip on the common heartbeat. | Done (commit `perf(api): resolve the edit-lock holder from the session`). Re-confirm flat DB load under many concurrent editors during rollout. |

| 27 | **Edit-lock web pen: flag-on Playwright + route-integration coverage** | From edit-lock M2. **(a) RESOLVED in M6** — `plan-detail.gating.test.tsx` is a route-level test (mocked query hooks) proving `canEditSchedule = canWrite && holdsPen` gates every schedule affordance while the Contributor progress path stays enabled, a 423 drops to read-only, and the reposition seam picks the right hook (also closes #24d). **(b) RESOLVED** — the flag-on `pen-smoke.spec.ts` runs the single-actor lifecycle (Start → write under enforcement → Stop → read-only), and `pen-handoff.spec.ts` now covers the **multi-actor** peer hand-off (two users in one org via invite→accept: A takes the pen, Planner B requests, A hands over, pen moves to B). | Resolved — single- and multi-actor pen journeys run on the `test:e2e:edit` harness. | Done. |

| 28 | **TSLD canvas ring/stroke colour treatment** | From the D5 link-legality UX + a11y reviews. **(a)** The **legal** drop-target ring during a link-draw is visually identical to the ordinary **selection** ring (`paint.ts` — both `palette.selection`, solid, 2px), so two rings with different meanings can appear in the same style at once (predates D5). **(b)** The **illegal** ring reuses `palette.critical` (`--color-destructive`), the same token as the CPM critical-path **bar fill** (`paint.ts`), so an illegal drop hovered over a critical-path activity draws red-on-red — weaker contrast exactly where the signal matters, and overloads one colour for two meanings. **(c)** `--color-destructive` is documented (`globals.css`) as tuned for button surfaces; its use as a **state-border/stroke** on the canvas (the critical-bar outline too) wants a contrast check vs `--color-destructive-text` in both themes. | Cosmetic/robustness; the illegal ring is still distinguishable by its dash (colour + pattern, WCAG 1.4.1 holds), so not an AA failure. | Give the legal drop-target ring a distinct treatment from selection; pick a canvas "danger stroke" token distinct from the critical-bar fill; verify destructive-token stroke contrast in both themes when the canvas palette is next revised. |

| 29 | **No auto-deploy — releasing an image doesn't ship it** | The release pipeline builds and pushes `web`/`api` images to GHCR (each tagged with its own version + `latest`, ADR-0027), but nothing tells the running host to pull them — the self-hosted Docker/Dockge stack keeps serving its cached image until someone runs `docker compose pull && up -d` (or clicks Dockge **Update**). This bit us once: production sat on `web:0.12.0` for several releases (missing the whole Project Explorer, 0.14.0+) because the stack was "updated" without the pinned tag being bumped and without a pull of a moving tag. Redeploy is now documented (`docs/DEPLOYMENT.md` → "Deploying a release to a self-hosted host"), and prod tracks `:latest`, but the pull step is still manual. | A shipped release can silently not reach users until someone manually redeploys; easy to believe you're on the latest when you aren't. | Add a pull trigger: a GHCR **registry webhook** (or a Watchtower / `dockge`-side poll) that pulls + recreates on a new `:latest`, or a deploy job/step that calls the host after a successful publish. Record the chosen mechanism (and its auth) in `docs/DEPLOYMENT.md`; relates to #5 (hosting platform). |

| 30 | **Canvas-first workspace fast-follows (ADR-0030 M1–M5 reviews)** — _(b) `useMenuTrigger`, (d) hidden-pane rAF pause, (h) landmark-unique resolved 2026-07-13; (a) intentionally left as a `matchMedia` transition side-effect_ | Non-blocking items from the canvas-first plan workspace reviews (accessibility/ux/performance/component), deferred because they are systemic or cheap-later rather than blocking the flag-off slice: (a) the app-shell still reads viewport with a hand-rolled `matchMedia` effect (`app-shell.tsx`) while the workspace uses the new shared `useMediaQuery` — migrate the shell onto it so there is one responsive-query hook; (b) the menu-trigger open/anchor/`aria-expanded` glue is now hand-written in both the rail row actions and `PlanActionsMenu` — extract a `useMenuTrigger` hook; (c) the mobile Diagram/Activities `WorkspaceViewToggle` (`radiogroup`) and the pen banner's segmented controls are both roving-`tabindex` single-selects — extract a shared `Tabs`/`SegmentedControl` primitive; (d) below `md` the hidden canvas pane stays mounted (`hidden`), so its render/rAF loop can keep running unseen — pause the TSLD rAF when its pane is `hidden`; (e) the panel-height clamp recomputes against `bodyHeight` on every workspace resize (`plan-workspace.tsx`) — fine at current scale but an O(n) re-clamp worth culling if it ever shows on a profile; (f) `usePlanWorkspaceModel` is covered only through the two route layouts, not directly — add a focused hook test; (h) the docked activity panel's `<section aria-label="Activities">` shares its accessible name with the `ActivitiesTable`'s inner DataTable scroll `region` (also "Activities"), so once the table has rows a screen-reader user hears two "Activities" landmarks (axe `landmark-unique`, a best-practice rule outside WCAG 2a/2aa, so not an AA failure) — give the section a distinct label (e.g. "Activities panel") or drop its landmark role; (g) the rail-prefs localStorage key changed shape (`width`→`size`) when it moved onto the shared primitive, dropping any persisted rail width once (accepted pre-1.0 per ADR-0030, both a11y + component reviewers concurred). | Minor; all current states meet WCAG 2.2 AA and sit within the perf budget. The hidden-pane rAF (d) is the one with a real (small) battery/CPU cost on mobile. | Migrate the shell onto `useMediaQuery`; extract `useMenuTrigger` and a `Tabs`/`SegmentedControl` primitive when the next consumer lands; pause the hidden-pane rAF; add a direct `usePlanWorkspaceModel` test; revisit the resize re-clamp only if it profiles hot. |

| 31 | **`VITE_CANVAS_TOOLBAR` ships dark during build + M5 fast-follows (ADR-0031)** — _(a) `SelectionActionsBar` mounted + (b) plan-chrome dialogs deduped into a shared `PlanChromeDialogs` 2026-07-13_ | The canvas-maximal chrome reclaim + future-proof Toolbar architecture (ADR-0031, spec `docs/specs/canvas-toolbar-architecture.md`) is built behind `VITE_CANVAS_TOOLBAR` (default-off, `apps/web/src/config/env.ts`), layered on ADR-0030's `VITE_CANVAS_WORKSPACE`. M0–M5 have landed (flag/ADR, `<Toolbar>` primitive, TSLD registry, pen-gating + floating selection bar, the toolbar-hosted layout, the flag-on Playwright journey, and the M4/M5 review remediation — recalc-command regression, context/UI-state memoisation, toolbar-control CVA, the below-`md` pane switch, and the three a11y blockers); only the **default-on flip awaits product sign-off**. **Deferred fast-follows:** (a) **resolved 2026-07-13** — the floating `SelectionActionsBar` (M3) is now **mounted**: the canvas writes the selected activity's viewport anchor to a `selectionAnchorRef` each frame (ADR-0026 D3, only on moved frames), the bar reads it on its own rAF (transform-positioned, change-detected) to follow pan/zoom, clamps itself inside the viewport edges, hands focus back to the listbox when it hides/unmounts while focused, and Edit/Delete open host-owned dialogs via a shared `ActivityCrudDialogs` (the "Set constraint" action was dropped as redundant with Edit). **Accepted trade-off (new fast-follow):** floating just above the selection overlays the region directly above it, so on a dense diagram it can cover the activity in the lane above for as long as the selection is active — accepted as a contextual, transient overlay; a future lane-aware / side placement is the fast-follow. (b) the three plan-chrome dialogs (Plan details / Baselines / Calendar) are **duplicated** between `plan-actions-menu.tsx` (flag-off) and `plan-workspace-toolbar.tsx` (flag-on) — extract a shared `PlanChromeDialogs`; (c) the toolbar layout's **collapsed** state is session-local (not persisted; height still persists) — thread a `defaultCollapsed`/separate key through `useResizablePanelPrefs`; (d) on an **empty/uncalculated** plan the frame/lens/help commands are **hidden** (`isVisible: hasDiagram`) rather than shown-disabled-with-reason as the spec's edge-case prefers — accepted for now since the empty plan still surfaces its only relevant actions (Add activity + Recalculate), but reconcile spec↔code (either disable-with-reason, or update the spec) when the empty-state copy is next revised; (e) non-blocking a11y recommendations from the M5 audit — the toolbar doesn't `.focus()` the new roving stop when a `ResizeObserver` demote unmounts the focused button mid-session (rare; falls back to `<body>`); `aria-orientation="horizontal"` while Up/Down are also wired (harmless superset); the segmented zoom presets are `aria-pressed` buttons rather than a `radiogroup`; and a manual NVDA/VoiceOver pass on the `CompactPenStatus` live-region + Start/Stop/Take-over sequence is still owed. | Divergent code paths coexist behind the flag until the flip; three layered flags (`VITE_CANVAS_WORKSPACE` → `VITE_CANVAS_TOOLBAR`); the dialog duplication can drift. | Flip `VITE_CANVAS_TOOLBAR` default-on once signed off (**done**); mount the selection bar (**done**), dedup the dialogs (**done**); still to do: a lane-aware / side placement for the floating bar so it never covers the lane above, persist the collapsed state, reconcile the empty-state hide-vs-disable, and clear the non-blocking a11y recommendations as fast-follows once the layout has soaked. Rollout tracked in the flag comment (`env.ts`). |

| 32 | **`btree_gist` extension install needs `CREATE`-on-database at deploy (ADR-0036, M1)** | The M1 calendar-shift migration (`20260715120100_calendar_shift_model`) runs `CREATE EXTENSION IF NOT EXISTS "btree_gist"` — the **first** `CREATE EXTENSION` in the project — to back the GiST `EXCLUDE` non-overlap constraints on shifts/exception windows. Under ADR-0018 the self-migrating container runs `prisma migrate deploy` as the app DB role; a least-privilege managed Postgres role may lack `CREATE`-on-database. `btree_gist` is a **trusted** extension on PG13+, so a role with `CREATE` can install it, and the CI `image` smoke-boot exercises the whole migration via the entrypoint successfully — but this has not been run against a locked-down managed instance. | If the prod migration role lacks `CREATE`-on-database, `migrate deploy` aborts at container startup, blocking the entire release (not just calendars). | Before the first M1 deploy to a managed host, confirm the migration role has `CREATE`-on-database, or pre-install `btree_gist` out-of-band (superuser) so `IF NOT EXISTS` is a no-op. Record the chosen approach in `docs/DEPLOYMENT.md`. |

| 33 | **M1 minute-rework non-blocking review nits (ADR-0036)** | Non-blocking items from the M1 specialist reviews, deferred as cheap-later: (a) **security** — `durationDays` has `@Min(0)` but no `@Max`, and the new `× 1440` conversion lowers the `INTEGER` overflow threshold to ~1.49 M days; a huge value currently 500s (opaque `INTERNAL_ERROR`, no leak) instead of a clean 400 — add `@Max(3650)` to `create/update-activity.dto.ts` to match the `lagDays` pattern; (b) **api/db** — `MINUTES_PER_DAY = 1440` is redeclared locally in ~~6 files instead of importing the exported constant from `schedule/day-compat-calendar.ts` — centralise; (c) **api** — the read-side `Math.round(minutes / 1440)` silently rounds; harmless while every write is integer-day-constrained, but add a dev-only assert/log for `minutes % 1440 !== 0` once M3 makes non-day-aligned minutes reachable; (d) **backend-perf** — constraint `resolve()` is recomputed up to 3× per constrained activity across the forward/effective-Visual/backward passes — memoise the resolved constraint once per `computeSchedule`; (e) the `duration_minutes` DB `DEFAULT 480` (8 h) no longer equals "one working day" (the old `duration_days DEFAULT 1` = 1440) — defensive-fallback only (the service always sets it explicitly), note in the ADR. | All minor: (a) is a robustness/DoS-annoyance (clean 400 vs opaque 500); (b)/(c)/(e) are maintainability; (d) is a constant-factor CPU cost that multiplies exposure to the (now-fixed) calendar-walker cost. | Pick up (a)/(b) opportunistically; do (c)/(d) alongside the M3 lag-calendar wiring (when non-day-aligned minutes and per-edge calendars land); (e) is a one-line ADR note. |
| 35 | **M6-F7 float-&-critical settings review fast-follows (ADR-0035 §17/§18/§20)** | Non-blocking items from the F7 specialist reviews (ux/component/accessibility), deferred as section-wide or cheap-later rather than blocking the flagged slice: (a) **section-wide settings grouping** — the plan "Schedule" section now stacks five settings (Calendar / Recalc mode / Expected-finish / and F7's three float-critical controls) loosely with `mt-3` and no visible sub-heading; F7 groups its three for AT via a `fieldset`/sr-only-`legend` (edit) + `aria-label`ed `dl` (read-only), but a visible heading + tighter grouping was deliberately NOT added to avoid a lone sub-heading the four siblings lack — a whole-section "group + head the settings" pass (and the `mt-3`→`mt-6` spacing nudge the ux review flagged) is owed; (b) **shared labeled-select primitive** — `OptionSelect` in `PlanScheduleSettings.tsx` is the next copy of the `<Label>+<Select>+hint+error` shape after the dependency dialogs (overlaps #21(e)/#34(b)) — fold all call sites into the `SelectField` primitive when it lands; (c) **saving-state not announced** — the "Saving…" hint swap isn't in a live region, so only the final success/error announces (shared with the recalc/expected-finish siblings via `useOptimisticSelect`) — a shared-hook enhancement if busy-state announcements are wanted; (d) **error-path test gap** — F7 now tests the error/rollback + busy paths, but the recalc-mode/expected-finish siblings still don't, and `use-optimistic-select.ts` has no direct unit test. | All minor; F7 itself meets WCAG 2.2 AA and its states are tested. (a)/(b) are maintainability/consistency; (c) is a shared-hook nicety; (d) is a coverage gap on the siblings, not F7. | Do the section-wide grouping/heading/spacing pass when the plan-settings area is next revised; extract `SelectField` (folding #21(e)/#34(b)) at the next `<Select>` consumer and migrate `OptionSelect` onto it; add busy-state announce to `useOptimisticSelect` if wanted; backfill the error/busy tests for the sibling pickers + a direct `use-optimistic-select` test. |
| 36 | **WBS-summary soft-delete subtree cascade (ADR-0038, M5-epic F5)** — **RESOLVED in M5-epic F7.5.** | ADR-0038 decides that soft-deleting a `WBS_SUMMARY` **cascades** to its `parent_id` subtree in one `delete_batch_id`, upholding the "no active row under a deleted ancestor" invariant. F5 shipped the schema/validation but `HierarchyLifecycleService` cascaded only Client→Project→Plan→Activity. **Fixed:** the activity branch of `cascadeSoftDelete` now resolves the active `parent_id` subtree breadth-first (a leaf is just itself; a summary sweeps its descendants) and stamps every row + every incident link with the one batch id, so `restoreBatch` reactivates the subtree together. The restore guard was also hardened symmetrically: an activity restores only while **both** its plan and (if grouped) its WBS-summary parent are active, so a separately-deleted child can't come back under a still-deleted summary. | Resolved — no direct-API path can leave an active child under a soft-deleted summary; the invariant holds on both delete and restore. | Done (service specs added for the subtree cascade and both restore-guard directions). Landed before the F8 web surface makes summaries planner-creatable. |
| 37 | **WBS canvas summary bars + navigator-tree nesting deferred (ADR-0038, M5-epic F8)** | F8 shipped the WBS **form surface** — the Type picker offers `WBS_SUMMARY`, and a flag-gated WBS **parent picker** nests any activity under a plan summary (round-tripping `parentId` through create/update, behind `VITE_ADVANCED_ACTIVITY_TYPES`). The plan's explicitly **"deferrable" larger piece** was not built: (a) rendering a **summary span-bar** (and the LOE span-bar) on the TSLD canvas over the branch it heads (ADR-0026/0030 — a larger canvas piece); (b) showing the `parentId` hierarchy as **visual nesting in the Project Explorer / activities view** (indented tree rows under their summary, ADR-0029). Today the parent is set via a Select and the roll-up shows only in the activity's own dates. | Low: the WBS feature is fully usable via the form (create a summary, nest activities, Recalculate, dates roll up) — only the at-a-glance _visualisation_ of the grouping is missing. Now on the **default-on** surface (`VITE_ADVANCED_ACTIVITY_TYPES` flipped on), so a planner sees the type/parent pickers but not a canvas summary bar or nested tree. | Add summary/LOE span-bar rendering to the canvas mapping seam (a pure `RenderActivity` addition + painter branch, like the constraint-pin/overlap cues) and indented nesting to the activities table/navigator, with component + a11y (WCAG 2.2 AA) tests, when the canvas rendering work is next scheduled. |
| 34 | **M3 lag-calendar non-blocking review fast-follows (ADR-0036 §6)** | Non-blocking items from the M3 specialist reviews, deferred as cheap-later: (a) **backend-perf** — the existing 500-node recalc smokes (`schedule.e2e-spec.ts`) exercise only the default (`PROJECT_DEFAULT`) lag path, which is zero-overhead by construction; there's no structural smoke at scale with, say, 500 edges _all_ carrying `TWENTY_FOUR_HOUR`, the one path whose per-edge cost actually changed (0 → a few binary-search calendar calls). Adding one (mirroring the calendar-load smoke) would turn "O(log) by inspection" into a measured fact. The engine unit test already proves _termination_ of a single ±11-year elapsed lag (`compute.lag-calendar.spec.ts` N16 case), so this is about the many-edge axis, not the huge-lag axis. (b) **component** — the manual `<Label>` + `<Select>` + hint-`<p>` block is now copy-pasted four times across the two dependency dialogs (Type ×2, Lag calendar ×2); the repo already has `TextField`/`TextareaField` in `components/ui/form.tsx` for exactly this shape — a sibling `SelectField` primitive (with `useId()`-generated hint ids) would collapse all four call sites and remove the hand-wired `dependency-lag-calendar-hint` / `edit-dependency-lag-calendar-hint` ids. Overlaps #21(e), which already asks for a shared `SelectField`. | Both minor: (a) is a coverage gap (the code is sound by reasoning + the termination test), (a) affects only the rare all-24H plan; (b) is maintainability — a fifth copy is the trigger to extract. | Add the all-`TWENTY_FOUR_HOUR` scale smoke next to the existing calendar-load smoke when the e2e is next touched; extract `SelectField` (folding in #21(e)) when the next `<Select>`+`Label` consumer lands, and migrate the four dependency-dialog call sites onto it. |
| 38 | **Activities row actions overflow → `Menu` primitive (M7-F6 ux review)** | The activities table (`ActivitiesTable.tsx`) renders its per-row actions as always-visible ghost buttons; M7-F6 gated a fifth ("Resources", behind `VITE_RESOURCES`) onto that row, tipping Logic/Progress/Edit/Delete to five and widening the crowding at `sm`/`md`. `docs/UX_STANDARDS.md` ("Row / node actions") + CLAUDE.md §12 require dense row actions to move behind the hand-rolled APG `Menu` primitive (`components/ui/menu.tsx`) — hover-`⋯`/right-click/keyboard/long-press, never hover-only. The resources feature followed the table's existing four-button convention rather than change it; migrating the whole cell to `Menu` is a separate refactor of a heavily-tested component, orthogonal to the resources feature. | Low while `VITE_RESOURCES` is default-off (four buttons, the pre-existing state); the fifth only appears once the flag is on. The pattern already technically exceeds the "dense actions use a Menu" standard at four. | Migrate the `ActivitiesTable` actions cell to the `Menu` primitive (a single overflow trigger listing Logic/Progress/Resources/Edit/Delete, role-gated) with component + a11y tests, before flipping `VITE_RESOURCES` on — do it as its own change so the table's action tests move with it. |
| 39 | **Resource-assignment writes are not plan-edit-lock (pen) gated (M7-F3 security review)** | Activity definition writes go through `PlanEditLockService.assertHoldsPen` (ADR-0028); the resource **assignment** write paths (`resource-assignment.service.ts` create/update/remove, added in the M7.1 resources epic) never adopted the pen-gate. Benign while assignments were pure bookkeeping, but since **M7 rung 4 (ADR-0040)** an assignment units/rate edit with an `editedField` now recomputes and persists the **owning activity's `duration_minutes`** (a scheduling-surface mutation) — so a planner can move an activity's duration without holding the plan's edit-lock, an asymmetry with the activity write path. Optimistic locking + RBAC + org scope still fully protect correctness; only the single-editor pen invariant is bypassed on this path. | Low: the pen-lock is itself flag-gated (`PLAN_EDIT_LOCK_ENFORCED`, an ops rollout switch, default off) and the recompute only fires once a driving assignment carries a rate (the whole duration-type feature is dark by default); correctness (no lost update / IDOR) is unaffected. | Thread `assertHoldsPen(principal, activity.planId, org.id)` into the resource-assignment create/update/remove paths (resolve the plan from the activity), mirroring the activity write path, with a 423 e2e — do it when the pen-lock rollout or the duration-type web surface (M-DT.3) lands, whichever first. |
| 41 | **No per-activity external-driven badge (ADR-0043 M1, IPD-F5 ux review)** | The engine computes a per-activity `externalDriven` flag (`EngineResult.externalDriven`), but ADR-0043 M1 deliberately persists only the plan-level `externalDrivenCount` (no `external_driven` activity column, unlike `constraintViolated`, which gets a per-row "Conflict" badge in `ActivitiesTable`). So a planner who sees "Externally driven: 3" on the summary strip cannot tell **which** three activities without manually comparing each activity's external dates against its computed dates. Soft/observability-only by design for M1 (the count answers "is anything gated?"), but the drill-down is missing. | Low while `VITE_INTER_PROJECT_DATES` is default-off; once the surface is considered for default-on, the count-without-drill-down is a real usability gap for anyone using external dates. | Before flipping `VITE_INTER_PROJECT_DATES` on, either persist a per-activity `external_driven` engine-owned column (like `constraint_violated`) and add an "External" row badge to `ActivitiesTable` (mirroring the Conflict badge, with component + a11y tests), or expose the per-activity flag on the recalc response and surface it route-composed — decide the persistence vs. recalc-only trade-off at that point. |
| 40 | **EV2a cost-input follow-ups (EV2a security review)** | Two non-blocking items from the EV2a security review, deferred to EV2b/EV3: (a) **money overflow guard** — the money write DTOs (`create/update-activity`, `create/update-assignment`: `budgetedExpense`/`actualExpense`/`budgetedCost`/`actualCost`, and the `costPerUnit` rate) validate `@IsInt/@IsNumber @Min(0)` but have no `@Max`; a value above `2^53` minor units (~~£90tn) would silently lose precision at the `Number(bigint)` wire boundary rather than 422-ing. (b) **Contributor progress-field wiring** — `actualCost`/`actualExpense`/`actualUnits`/`physicalPercentComplete` are documented as Contributor-writable progress, but EV2a only wired them into the Planner-gated `activity:update`/`resource:assign` DTOs, not the Contributor `activity:update_progress` path. | Low: (a) is astronomically out of range for construction cost and the DB column is `BIGINT` (no DB overflow); (b) is **fail-closed** (too restrictive, never over-exposed) — a Contributor simply can't enter cost actuals yet, no security impact. | (a) add `@Max(Number.MAX_SAFE_INTEGER)` (or a tighter business ceiling) to the money write DTOs, matching the N22/N23 boundary-reject pattern; (b) decide the Contributor cost-progress surface and wire the actual-cost/physical-% fields into the progress path when EV2b/EV3 builds the cost entry UX. |
| 42 | **Shared `SelectField` composite not extracted (M7 rung-5 component review)** | The `Label` + `Select` + `aria-describedby`-linked help-text idiom is now hand-assembled ~6× — `ActivityFormDialog` (%-complete type, cost accrual, constraint type, …) plus the ADR-0044 §3 loading-curve select (assign form + row) and the histogram bucket-size select. Each repeats the `useId()` wiring the shared `TextField`/`CheckboxField` already encapsulate. | Low: purely internal duplication; every instance is individually correct (labelled + described). Drift risk — a future a11y fix must be applied in every copy. | Extract a shared `SelectField` composite (label + `Select` + optional error/help wired via `aria-describedby`) mirroring `TextField`, and migrate the call sites. Overlaps the earlier `SelectField`-composite notes folded into the aggregate review rows (#21/#34/#35); fold those in when this lands. |
| 43 | **Resource-histogram bucket size not URL-deep-linkable (M7 rung-5 ux review)** | The histogram's Day/Week/Month `granularity` is component-local `useState`, so it can't be shared/bookmarked and resets to the WEEK default each open — unlike the URL-state convention (TanStack Router) the app uses for other view selections. | Minor: a planner re-picks the bucket size each time; nothing is lost. | Lift `granularity` into the plan route's search params (like other URL-derived view state) so the histogram opens on, and can be linked at, a chosen bucket size. |
| 44 | **Steps/curve surfaces not context-gated (M7 rung-5 ux review)** | Two related flag-coupling gaps behind `VITE_ACTIVITY_STEPS` / `VITE_RESOURCE_CURVES`: (a) weighted **Steps** only feed anything when Earned Value is on (the rolled-up physical % is an EV input), yet the Steps action shows independently of `VITE_EARNED_VALUE`; (b) the **loading-curve** picker is offered even for zero-span activities (milestones), where a curve has nothing to distribute over. | Low: both are dark surfaces during rollout; a planner could set values that are inert until the sibling feature is on / the activity has a span. | When these flags flip default-on, couple Steps visibility to the EV surface (or document the standalone value), and hide/curtail the loading-curve picker for zero-span activity types. |

## Principles for managing debt

- Prefer paying debt down opportunistically while touching nearby code.
- Never add **undocumented** debt: if you take a shortcut, add a row here.
- Security- and data-integrity-related debt is prioritised above convenience.
