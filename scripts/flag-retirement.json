{
  "$comment": "Generated once, maintained by hand. See docs/adr/0084-feature-flag-retirement.md.",
  "horizonDays": 30,
  "batches": {
    "batch-1": {
      "due": "2026-08-11"
    },
    "batch-2": {
      "due": "2026-08-18"
    },
    "batch-3": {
      "due": "2026-08-25"
    },
    "batch-4": {
      "due": "2026-09-01"
    },
    "batch-5": {
      "due": "2026-09-08"
    },
    "batch-6": {
      "due": "2026-09-15"
    },
    "batch-7": {
      "due": "2026-09-22"
    },
    "batch-8": {
      "due": "2026-09-29"
    },
    "batch-9": {
      "due": "2026-10-06"
    },
    "batch-10": {
      "due": "2026-10-13"
    },
    "batch-11": {
      "due": "2026-10-20"
    },
    "batch-12": {
      "due": "2026-10-27"
    },
    "batch-13": {
      "due": "2026-11-03"
    },
    "batch-14": {
      "due": "2026-11-10"
    },
    "batch-15": {
      "due": "2026-11-17"
    },
    "batch-16": {
      "due": "2026-11-24"
    },
    "batch-17": {
      "due": "2026-12-01"
    }
  },
  "retired": [
    {
      "flag": "VITE_NAV_TREE_CRUD",
      "constant": "NAV_TREE_CRUD_ENABLED",
      "enabled": "2026-07-12",
      "enabledSource": "docblock",
      "retiredOn": "2026-08-09",
      "batch": "batch-1"
    },
    {
      "flag": "VITE_CANVAS_TOOLBAR",
      "constant": "CANVAS_TOOLBAR_ENABLED",
      "enabled": "2026-07-13",
      "enabledSource": "docblock",
      "retiredOn": "2026-08-10",
      "batch": "batch-2",
      "note": "ADR-0088 D3, the first retirement on MERIT rather than on a date. Its flag-off branch was `Adr0030PlanWorkspace`, a ~257-line second implementation of the plan surface that no shipped bundle could select and that ADR-0080 had already attributed a user-facing defect to. `plan-workspace.tsx` goes 334 lines to 24. The flag-off parity suite (plan-workspace.test.tsx) and the flag-off harness (playwright.workspace.config.ts + e2e-workspace/) are deleted with their subject (ADR-0084 D5); the splitter assertion only that harness carried was PORTED to e2e-toolbar/toolbar.spec.ts, and plan-detail.gating.test.tsx was RE-HOSTED onto the surviving layout rather than deleted \u2014 it was pen-gating coverage that merely happened to live there."
    },
    {
      "flag": "VITE_CALENDAR_SHIFT_EDITOR",
      "constant": "CALENDAR_SHIFT_EDITOR_ENABLED",
      "enabled": "2026-08-01",
      "enabledSource": "docblock",
      "retiredOn": "2026-08-10",
      "batch": "batch-10",
      "note": "ADR-0088 D3, batch-2 retirement M1. Flag-off selected seven weekday checkboxes \u2014 a control that can say only WHETHER a day works, so it could not express the split shifts and part days ADR-0036 stores and ADR-0067 built the editor to author. Deleted with the flag: `WeekdayToggleGroup`, the `weekIsSimplified` advisory, the flag-off submit branch, and `workingWeekdays` from `calendarFormSchema` \u2014 the last because keeping a validated field with no control left to satisfy it is the ADR-0067 M4 dead end verbatim. `use-calendars.ts` keeps its `workingWeekdays` members and the flatten docblock deliberately: the API still ACCEPTS a mask, and that comment is the surviving record of why sending one is destructive. Its two `Omit<CalendarFormValues,'workingWeekdays'>` aliases were retyped \u2014 `Omit` of an absent key compiles and asserts nothing, the one site typecheck could not catch. `playwright.calendar-shifts.config.ts` is KEPT (it pinned 'true', i.e. drove the surviving world); only its one-line `env` pin went. No user-visible change: the flag was compiled on in every published image (ADR-0088 D1)."
    },
    {
      "flag": "VITE_LIBRARY_SCOPING",
      "constant": "LIBRARY_SCOPING_ENABLED",
      "enabled": "2026-07-26",
      "enabledSource": "docblock",
      "retiredOn": "2026-08-10",
      "batch": "batch-8",
      "note": "ADR-0088 D3, batch-2 retirement M2. Flag-off selected a raw `<select>` in FIVE two-arm picker sites (ActivityFormDialog, ActivityCalendarField, PlanCalendarPicker, ResourceFormDialog, ActivityResourcesPanel) and suppressed a sixth GUARD-only site (ResourceFormDialog's Group picker, whose flag-off branch was `: null` \u2014 a Class B shape, not a selection). The register said 'four pickers'; that was detect-alternative-surfaces.mjs's output, and it structurally cannot see a ternary wrapping a <div> or one behind an intervening comment \u2014 which is why the real backstop for this retirement was deleting the constant from env.ts and letting typecheck find every missed reference. `playwright.library.config.ts` is KEPT and only its ONE pin line removed: that config pins SEVEN keys including VITE_SCHEDULING_MODES:'false', so deleting the env block wholesale (the obvious 'mirror the other milestone' move) would have silently armed scheduling modes in a journey written to assert the plain surface \u2014 a live flag-off harness for a DIFFERENT flag, disarmed as collateral. No user-visible change: compiled on in every published image (ADR-0088 D1)."
    },
    {
      "flag": "VITE_ACTIVITY_EDITOR_TABS",
      "constant": "ACTIVITY_EDITOR_TABS_ENABLED",
      "enabled": "2026-07-29",
      "enabledSource": "docblock",
      "retiredOn": "2026-08-11",
      "batch": "batch-9",
      "note": "Its deferral trigger fired: `epic-touch: activity editor`. The register was right that retiring it ALONE collected none of the payoff \u2014 the legacy monolith would have survived as the create surface \u2014 so the dialog-unification epic closed all ten create/edit divergences and extracted eleven shared field groups first, and this retirement lands inside that epic rather than as a flag chore. Both flag-off Playwright harnesses (sub-day, assignment-lag) were CONVERTED to the shipping surface before the flag went (ADR-0084 D5), which is the ADR-0084 batch-1 lesson applied in advance. Both derived children lose their conjunct: the stranding it guarded \u2014 an editor intent with no editor to open \u2014 is now unreachable by construction."
    },
    {
      "flag": "VITE_CANVAS_WORKSPACE",
      "constant": "CANVAS_WORKSPACE_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "retiredOn": "2026-08-17",
      "batch": "batch-12",
      "note": "ADR-0088 D3 \u2014 the estate's LAST Class A flag, retired in the dedicated flag-cleanup pass the product owner called for on 2026-08-12. Its flag-off branch was `LegacyPlanLayout`, a ~270-line long-scrolling second implementation of the plan surface that no shipped bundle could select (ADR-0088 D1); `plan-detail.tsx` goes 391 lines to 67. `CANVAS_AUTHORING_ENABLED` loses its `&& CANVAS_WORKSPACE_ENABLED` conjunct \u2014 a derived child outliving its parent, ADR-0084 D4's harmless direction. THE COST WAS THE HARNESSES, NOT THE CODE: seven configs pinned this OFF and all 27 of their specs needed conversion \u2014 base 8 of 17, activity-editor 11, edit 3, sub-day 2, and one each for notes, programme and assignment-lag. Nothing was deleted as redundant and nothing had the legacy layout as its SUBJECT; the only suite that ever did went with VITE_CANVAS_TOOLBAR. Two recurring facts became one shared `e2e/workspace.ts`: expanding the collapsed activities panel (wait on the panel TOGGLE \u2014 `DataTable` renders no `<table>` at all when there are no rows, and `isVisible()` is a snapshot rather than a wait), and reading the computed schedule from the API instead of the `ScheduleSummaryStrip` label, which is now inside a band-floored toolbar chip that legitimately withholds itself at these viewports. TWO ASSERTIONS WERE DELETED RATHER THAN CONVERTED, and both are the derivation biting: `tsld.spec.ts` asserted 'Recalculate the schedule to plot\u2026' before recalculating, and with authoring now unconditional the canvas is live from the first activity (ADR-0032), so that state no longer exists. The SECOND is the same cause found a day later by CI rather than by reading, which is the point worth keeping: `schedule.spec.ts` asserted a plan had no computed schedule before the planner pressed Recalculate, and with the canvas mounted the ADR-0032 auto-recalc schedules it unprompted, so the assertion was a race against a debounce. It passed on Chromium and failed on firefox and webkit alone \u2014 a timing difference wearing a browser difference's clothes \u2014 and the local sweep could not see it, because this container ships no firefox or webkit binary and all of their cases fail there for an unrelated reason. One correct call applied to a spec and not its neighbour, which is the shape the register keeps recording; the honest read is that converting 27 specs found the first instance by reasoning and missed the second, and only something running the real thing on the real engines closed the gap. Three unit tests mocked `CANVAS_WORKSPACE_ENABLED: true`; those keys are deleted rather than left inert, which is the residue class the architecture review flagged \u2014 a stale key in a `vi.mock` factory still compiles and still passes. The pen pins in base/programme/notes are untouched: a separate question, TECH_DEBT #121. No user-visible change \u2014 the flag was compiled on in every published image (ADR-0088 D1)."
    },
    {
      "flag": "VITE_NAV_TREE",
      "constant": "NAV_TREE_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "retiredOn": "2026-08-18",
      "batch": "batch-16",
      "note": "Retired by the organisation-landing epic (ADR-0098 M0), before it rewrote the flag's own host file \u2014 a screen should not be redesigned while a second, unreachable version of it sits in the same component. Two branches went: AppShell's alternative in `authed-layout.tsx`, and a card in `org-home.tsx` reading \"The schedule editor arrives in an upcoming update\", about a year after the editor shipped. Nobody had ever seen either \u2014 a `VITE_` constant is inlined at build time and no published image passes one (ADR-0088 D1) \u2014 which is the point: an unreachable screen does not go stale harmlessly, it goes stale INVISIBLY, and the next person to design the route reads a screen that does not exist. No flag-off parity suite and no pinned harness existed to convert (searched: no playwright*.config.ts pinned it, no unit test mocked the constant), so typecheck was the instrument, as the VITE_LIBRARY_SCOPING retirement note predicted. **AND THE CLASSIFICATION WAS WRONG.** It was filed class B \u2014 a one-line guard \u2014 while `authed-layout.tsx:15` was `if (NAV_TREE_ENABLED) return <AppShell />;`, an early return selecting between two whole layouts: the class A shape by ADR-0088's own definition. `check-flags.mjs`'s equality against classACap could not see it, because `detect-alternative-surfaces.mjs` matches ternaries returning JSX and is blind to early returns \u2014 which `$classA-note` in this file already says. So the measured class A count was 2 when the true count was 3, and the cap that is supposed to ratchet down was guarding a number it could not compute. Retiring in this commit keeps the detected count at 0 and leaves classACap untouched; raising it for a flag that is leaving would have been the wrong direction. The gate's blind spot is the finding, not this flag."
    }
  ],
  "flags": {
    "VITE_ACCOUNT_SETTINGS": {
      "constant": "ACCOUNT_SETTINGS_ENABLED",
      "enabled": "2026-08-05",
      "enabledSource": "docblock",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ACTIVITY_CALENDAR": {
      "constant": "ACTIVITY_CALENDAR_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ACTIVITY_COPY_PASTE": {
      "constant": "ACTIVITY_COPY_PASTE_ENABLED",
      "enabled": "2026-08-08",
      "enabledSource": "docblock",
      "batch": "batch-17",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ACTIVITY_EDITOR_CONVERGENCE": {
      "constant": "ACTIVITY_EDITOR_CONVERGENCE_ENABLED",
      "enabled": "2026-07-29",
      "enabledSource": "docblock",
      "batch": "batch-10",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ACTIVITY_STEPS": {
      "constant": "ACTIVITY_STEPS_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-6",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ADVANCED_ACTIVITY_TYPES": {
      "constant": "ADVANCED_ACTIVITY_TYPES_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-15",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ADVANCED_CONSTRAINTS": {
      "constant": "ADVANCED_CONSTRAINTS_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ASSIGNMENT_LAG": {
      "constant": "ASSIGNMENT_LAG_ENABLED",
      "enabled": "2026-08-02",
      "enabledSource": "docblock",
      "batch": "batch-11",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_AUDIT_FILTERS": {
      "constant": "AUDIT_FILTERS_ENABLED",
      "enabled": "2026-08-04",
      "enabledSource": "docblock",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_AUDIT_LOG": {
      "constant": "AUDIT_LOG_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "docblock",
      "batch": "batch-15",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_AUDIT_SELF_SECURITY": {
      "constant": "AUDIT_SELF_SECURITY_ENABLED",
      "enabled": "2026-08-04",
      "enabledSource": "docblock",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_ACTIVITY_TYPES": {
      "constant": "CANVAS_ACTIVITY_TYPES_ENABLED",
      "enabled": "2026-07-20",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_AUTHORING": {
      "constant": "CANVAS_AUTHORING_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-13",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4.",
      "note": "Was derived from CANVAS_TOOLBAR_ENABLED as well until ADR-0088 D3 retired that flag. A retired parent drops its conjunct (ADR-0084 D4): nobody can turn off what no longer exists."
    },
    "VITE_CANVAS_AUTHORING_FLOW": {
      "constant": "CANVAS_AUTHORING_FLOW_ENABLED",
      "enabled": "2026-07-31",
      "enabledSource": "docblock",
      "batch": "batch-13",
      "derivedFrom": "CANVAS_AUTHORING_ENABLED",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4.",
      "note": "Moved batch-10 -> batch-13 on 2026-08-10, by the M0 derivation gate rather than by a re-plan. It is derived from CANVAS_AUTHORING_ENABLED (env.ts), whose own batch is 13 \u2014 so it was scheduled to retire three weeks BEFORE its parent, declaring the feature permanent while the parent could still switch it off. Assertion 5 existed to stop exactly this and could not see it, because `derivedFrom` was hand-maintained and this edge had never been recorded. Both flags are Class B `keep`, so nothing was actually scheduled and the fix costs nothing; the finding is that the gate was enforcing on 3 of 9 edges. Dropping the edge instead would have been forbidden \u2014 the edge is real (env.ts:1098-1099)."
    },
    "VITE_CANVAS_DATA_DATE": {
      "constant": "CANVAS_DATA_DATE_ENABLED",
      "enabled": "2026-08-07",
      "enabledSource": "docblock",
      "batch": "batch-17",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_DIRECT_MANIPULATION": {
      "constant": "CANVAS_DIRECT_MANIPULATION_ENABLED",
      "enabled": "2026-07-25",
      "enabledSource": "docblock",
      "batch": "batch-8",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_LENSES": {
      "constant": "CANVAS_LENSES_ENABLED",
      "enabled": "2026-07-19",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_LINK_ROUTING": {
      "constant": "CANVAS_LINK_ROUTING_ENABLED",
      "enabled": "2026-07-31",
      "enabledSource": "docblock",
      "batch": "batch-10",
      "derivedFrom": "CANVAS_DIRECT_MANIPULATION_ENABLED",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_LIVE_FEEDBACK": {
      "constant": "CANVAS_LIVE_FEEDBACK_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_MULTI_SELECT": {
      "constant": "CANVAS_MULTI_SELECT_ENABLED",
      "enabled": "2026-08-07",
      "enabledSource": "docblock",
      "batch": "batch-17",
      "derivedFrom": "CANVAS_DIRECT_MANIPULATION_ENABLED",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_NAV": {
      "constant": "CANVAS_NAV_ENABLED",
      "enabled": "2026-07-20",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_RESOURCE_VIEW": {
      "constant": "CANVAS_RESOURCE_VIEW_ENABLED",
      "enabled": "2026-07-20",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "derivedFrom": "RESOURCE_CURVES_ENABLED",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_SEARCH_NAV": {
      "constant": "CANVAS_SEARCH_NAV_ENABLED",
      "enabled": "2026-08-07",
      "enabledSource": "docblock",
      "batch": "batch-17",
      "derivedFrom": "CANVAS_LENSES_ENABLED",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_TIME_AXIS": {
      "constant": "CANVAS_TIME_AXIS_ENABLED",
      "enabled": "2026-07-27",
      "enabledSource": "docblock",
      "batch": "batch-9",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_CANVAS_VISUAL_LANGUAGE": {
      "constant": "CANVAS_VISUAL_LANGUAGE_ENABLED",
      "enabled": "2026-07-26",
      "enabledSource": "docblock",
      "batch": "batch-8",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_COST_ACCRUAL": {
      "constant": "COST_ACCRUAL_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-6",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_DESIGNED_CHROME": {
      "constant": "DESIGNED_CHROME_ENABLED",
      "enabled": "2026-07-26",
      "enabledSource": "docblock",
      "batch": "batch-8",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_DURATION_TYPES": {
      "constant": "DURATION_TYPES_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-6",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_EARNED_VALUE": {
      "constant": "EARNED_VALUE_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-5",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_ENTRY_ROUTES": {
      "constant": "ENTRY_ROUTES_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_EXPORT_PRINT": {
      "constant": "EXPORT_PRINT_ENABLED",
      "enabled": "2026-07-20",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_FLOAT_CRITICAL_SETTINGS": {
      "constant": "FLOAT_CRITICAL_SETTINGS_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_FLOAT_PATHS": {
      "constant": "FLOAT_PATHS_ENABLED",
      "enabled": "2026-08-02",
      "enabledSource": "docblock",
      "batch": "batch-11",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_GANTT_VIEW": {
      "constant": "GANTT_VIEW_ENABLED",
      "enabled": "2026-07-28",
      "enabledSource": "docblock",
      "batch": "batch-9",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_GUEST_SHARE_LINKS": {
      "constant": "GUEST_SHARE_LINKS_ENABLED",
      "enabled": "2026-07-21",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_INTER_PROJECT_DATES": {
      "constant": "INTER_PROJECT_DATES_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-5",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_NOTES": {
      "constant": "NOTES_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-15",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_PASSWORD_RESET": {
      "constant": "PASSWORD_RESET_ENABLED",
      "enabled": "2026-08-05",
      "enabledSource": "docblock",
      "batch": "batch-17",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_PLAN_EDIT_LOCK": {
      "constant": "PLAN_EDIT_LOCK_ENABLED",
      "enabled": "2026-07-12",
      "enabledSource": "docblock",
      "batch": "batch-2",
      "note": "Retired in batch 1 on 2026-08-09 and PUT BACK the same day, because CI caught what the plan did not: `apps/web/playwright.config.ts` pins this flag OFF for the entire BASE journey, and six editing specs (activities, baselines, dependencies x2, schedule x2) are written against that world \u2014 they click controls the unconditional pen now shades, and time out. Retiring it means converting those specs to take the pen, which is a slice of its own rather than a line deletion. ADR-0084 D5 named the unit parity suites and did not say a whole Playwright config can BE one; `check:flags` now refuses to retire a flag any config still pins.",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_PROGRAMME_SCHEDULING": {
      "constant": "PROGRAMME_SCHEDULING_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-15",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_PROGRESS_INGESTION": {
      "constant": "PROGRESS_INGESTION_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-16",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_RESOURCES": {
      "constant": "RESOURCES_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-5",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_RESOURCE_CURVES": {
      "constant": "RESOURCE_CURVES_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-6",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_RESOURCE_LEVELLING": {
      "constant": "RESOURCE_LEVELLING_ENABLED",
      "enabled": "2026-07-18",
      "enabledSource": "docblock",
      "batch": "batch-6",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_SCHEDULE_INTERCHANGE": {
      "constant": "SCHEDULE_INTERCHANGE_ENABLED",
      "enabled": "2026-07-20",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_SCHEDULING_MODES": {
      "constant": "SCHEDULING_MODES_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-14",
      "derivedFrom": "CANVAS_AUTHORING_ENABLED",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_SUB_DAY_DURATIONS": {
      "constant": "SUB_DAY_DURATIONS_ENABLED",
      "enabled": "2026-08-03",
      "enabledSource": "floor",
      "batch": "batch-15",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_TOOLBAR_QUICK_WINS": {
      "constant": "TOOLBAR_QUICK_WINS_ENABLED",
      "enabled": "2026-07-19",
      "enabledSource": "docblock",
      "batch": "batch-7",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_TSLD_EDITING": {
      "constant": "TSLD_EDITING_ENABLED",
      "enabled": "2026-07-12",
      "enabledSource": "docblock",
      "batch": "batch-3",
      "note": "Retired in batch 1 on 2026-08-09 and PUT BACK the same day, because CI caught what the plan did not: `apps/web/playwright.config.ts` pins this flag OFF for the entire BASE journey, and six editing specs (activities, baselines, dependencies x2, schedule x2) are written against that world \u2014 they click controls the unconditional pen now shades, and time out. Retiring it means converting those specs to take the pen, which is a slice of its own rather than a line deletion. ADR-0084 D5 named the unit parity suites and did not say a whole Playwright config can BE one; `check:flags` now refuses to retire a flag any config still pins.",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_UNDO_REDO": {
      "constant": "UNDO_REDO_ENABLED",
      "enabled": "2026-07-19",
      "enabledSource": "docblock",
      "batch": "batch-6",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    },
    "VITE_WBS_IMPROVEMENTS": {
      "constant": "WBS_IMPROVEMENTS_ENABLED",
      "enabled": "2026-07-30",
      "enabledSource": "docblock",
      "batch": "batch-10",
      "class": "B",
      "keep": "Guard-only: the flag GATES a capability rather than SELECTING between two implementations of one, so its absence cannot drift and there is no second product to keep in step. ADR-0088 D4."
    }
  },
  "$schema-note": "enabledSource: \"docblock\" = the date the flag itself records; \"floor\" = the earliest date this repository can prove, used for the fourteen flags whose real flip is recorded NOWHERE (ADR-0084). A floor can only make a flag look YOUNGER than it is, so retiring on one is conservative \u2014 never premature. It is not corrected by guessing.",
  "sizing": "A batch is sized by BLAST RADIUS. The enablement cohort only sets the EARLIEST date a flag may fall on (enabled + horizonDays); it does not set the batch. Re-spaced 2026-08-09 after the original cohort-only split put 17 flags and ~75 production files into a batch due in nine days \u2014 the largest batch on the second-earliest date.\nMEASURED per flag: production files referencing the constant, and the number of playwright*.config.ts files that PIN it. The pin count is the one nobody had: VITE_CANVAS_AUTHORING is pinned in 14 configs and VITE_SCHEDULING_MODES in 13, and CI proved a single pinned config costs six specs to convert, not a line.\nESTIMATED, and labelled as such: weight = files + 3x pins and a cap of 45 per batch. The cadence was fortnightly and is WEEKLY from batch-2 (product owner, 2026-08-09), which finishes the queue 2026-12-01 rather than 2027-03-23. Note what that does to the estimate: the batch COMPOSITION was fitted against a fortnight and now has a week, so the cap is the part now under test \u2014 halving the cadence doubles the rate of work without re-sizing a single batch. The weighting, the cap and the cadence all come from ONE completed retirement, so they are a hypothesis rather than a plan. `check:flags` tests it for us \u2014 if batch 2 takes longer than its week, the next batch date fails and forces a re-fit against a second data point. Re-fit it then; do not defend it.",
  "classA": {},
  "classACap": 0,
  "$keep-note": "CORRECTED 2026-08-14. All 52 keep reasons read \"Guard-only: ONE LINE of production code, so there is no second product to maintain\" \u2014 a factual claim, asserted 52 times, that nobody had checked and that is false for every flag sampled: NOTES_ENABLED has 8 non-test production files, SCHEDULING_MODES_ENABLED 8, CANVAS_LENSES_ENABLED 4. ADR-0088 D4s CONCLUSION survives untouched \u2014 the shape is still a guard \u2014 but the sentence defending it was wrong, so it now states the SHAPE argument (gates a capability vs selects between two implementations) rather than a line count nobody measured. Found by an architecture review of the flag-cleanup pass. The line count was never the argument; it was a proxy for it, and a proxy that is false is worse than no proxy.",
  "$classA-note": "ADR-0088 D2/D3. Authoritative \u2014 `scripts/detect-alternative-surfaces.mjs` is a TRIPWIRE that asserts `detected subset-of classA`, never the converse, because it can only under-detect (a flag branching by early return, by `const B = FLAG ? X : Y`, or through an indirection is invisible to it and is legitimately curated by hand). `classACap` is the MEASURED count and ratchets DOWN after each retirement; raising it needs an ADR.",
  "deferralTriggers": [
    "epic-touch: activity editor",
    "epic-touch: plan workspace",
    "flag-cleanup pass",
    "retirement in flight"
  ],
  "$flag-cleanup-note": "`flag-cleanup pass` was added to the vocabulary on 2026-08-12 by product-owner decision, which is exactly the form the note below asks for \u2014 a decision somebody makes, in a diff, rather than a free-text sentence in one flag's reason. It supersedes `epic-touch: plan workspace` for VITE_CANVAS_WORKSPACE: ADR-0090 FIRED that trigger and measured the cost (all seven harnesses fail a bare pin-flip, 27 specs), and the owner's call is that every flag stays ON and the estate is cleaned up in ONE batched pass rather than a flag at a time. That is a stronger reason than the epic's own off-ramp, and it is a real event rather than an indefinite opt-out \u2014 but note it is also a WIDER one, so a second flag deferred under it should be a deliberate choice and not a habit.",
  "$deferral-note": "ADR-0088 D3a. A Class A flag may be deferred past its batch date, but NOT by a bare date and NOT by `keep` \u2014 `keep` means 'Class B, guard-only, never retires', which is a written false statement about a Class A flag and corrupts the classification this register exists to defend. A deferral carries a trigger from `deferralTriggers` (a CLOSED vocabulary: adding a value is a decision somebody makes, in a diff) and a named TECH_DEBT row, so it is an event that will happen rather than an indefinite opt-out. This is the escape hatch the epic exists to remove, so it is bounded by construction."
}
